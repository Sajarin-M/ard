---
alwaysApply: true
---

## Bun

- Use Bun for package management and scripts.
- Install: `bun install`. Run: `bun run <script>` (dev, build, serve, typecheck, test).
- Add deps: `bun add <pkg>`; dev: `bun add -d <pkg>`.
